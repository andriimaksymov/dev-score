{
  "$comment": "CORS is handled exclusively by the Nest app (enableCors in main.ts, driven by FRONTEND_URL) so there is a single source of truth. Do not add static CORS headers here — they silently override the env-driven config."
}
